import React from "react";

interface Props {}

const Input = (props: Props) => {
  return (
    <div className="input">
      <div className="input__content">
        <textarea></textarea>
      </div>
    </div>
  );
};

export default Input;
